data:
  taglist: "test1,test2,test3"
  name: "test runner 123"
  obj:
    name: "foo-obj-name"
    env: $TESTVAR
    moreenv: foo-${TESTVAR}-$TESTVAR2
    list: [$TESTLIST,"bla",5, [$TESTLIST]]
    num: 1234
    nolist: ${TESTLIST}
    justvar: ${TESTVAR}
    notset: $NOTSETVAR
    alsonotset: ${NOTSETVAR}
  somelist:
    - one
    - two
    - tree
more-data:
  name: moredata